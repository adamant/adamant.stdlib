namespace system.comparison;

public max[T](a: T, b: T) -> T
    where T: Ordered
{
    return if a >= b => a else => b;
}

public min[T](a: T, b: T) -> T
    where T: Ordered
{
    return if a <= b => a else => b;
}
