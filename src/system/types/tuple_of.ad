namespace system.types;

using system.collections;

// Given a list of types return the tuple type with that as it's type parameter
// public fn tuple_of(types: List[type]) -> type
// {
//     return Tuple[params types];
// }
