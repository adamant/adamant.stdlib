namespace system.types;

using system.collections;

public fn repeat(t: type, count: size) -> List[type]
{
    let types = new List[type]();
    foreach _ in 1..count
        { types.add(t); }
    return types;
}
