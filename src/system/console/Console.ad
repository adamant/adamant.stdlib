public mut class Console
{
    public new() { }

    published safe fn write_line(mut self, line: String)
    {
        unsafe
        {
            print_string(line.bytes_count, line.bytes);
            let newline: String = "\n";
            print_string(newline.bytes_count, line.bytes);
        }
    }
}
