public mut class Console
{
    public new() { }

    published safe fn write_line(mut self, line: String)
    {
        unsafe
        {
            print_string(line.byte_count, line.bytes);
            let newline: String = "\n";
            print_string(newline.byte_count, line.bytes);
        }
    }
}
