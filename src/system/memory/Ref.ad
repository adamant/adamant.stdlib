namespace system.memory;

/// To determine the size of a reference to `T` use `sizeof[Ref[T]`.
///
/// This is needed becuase `sizeof[T]` gives the size of an object of type `T`.
public struct Ref[T]
    where T: class
{
    private let value: T = uninitialized;

    private init() { }
}
